import styled from 'styled-components'

const QuestionStyle = styled.View`
  flex-direction: row;
  justify-content: space-around;
  align-items: flex-end;
  border-width: 1;
  border-color: #d6d7da;
  padding: 20px;
`

export default QuestionStyle
